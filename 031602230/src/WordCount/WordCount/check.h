#pragma once
#include <string>
#include <iostream>
using namespace std;

class Check
{
public:
	bool check_isChar(char s1);
	bool check_isVal(char s1);
	bool check(string str);
	void testfile(string str);
};